version: '3.8'

services:
  yibo-ai:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./yibo-ai:/usr/share/nginx/html
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    restart: unless-stopped
    container_name: yibo-ai-static